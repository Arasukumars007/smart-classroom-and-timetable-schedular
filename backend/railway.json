{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "releaseCommand": "alembic upgrade head && python seed_data.py",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
